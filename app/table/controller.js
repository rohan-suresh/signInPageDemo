import Ember from 'ember';

export default Ember.Controller.extend({
  //actions: {
    //clickToExpand: function(zyBookCode, zyBookChapter) {
      //this.transitionToRoute('table', zyBookCode, zyBookChapter);
    //}

  //}
});

